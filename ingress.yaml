apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ywti-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ywti-service
            port:
              number: 80